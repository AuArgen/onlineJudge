package database

// addCppCourseContent seeds the "C++ from scratch" course lessons.
func addCppCourseContent(m map[string]lessonContentSeed) {
	m["cpp-first-program"] = lessonContentSeed{
		Summary: "Напишите свою первую программу на C++ и выведите текст на экран.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Программа — это последовательность команд, которую выполняет компьютер. На C++ команды записываются в файл с исходным кодом, а компилятор превращает его в исполняемый файл.

Начнём с самой известной программы в мире — она выводит на экран приветствие. Нажмите «Запустить» под кодом и посмотрите на результат.`},
			{Type: "code", Language: "cpp", Caption: "Ваша первая программа на C++ — попробуйте запустить", Content: `#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Разберём программу построчно:

• #include <iostream> — подключает библиотеку ввода-вывода: без неё std::cout работать не будет.
• int main() { ... } — главная функция: с неё начинается выполнение любой программы на C++.
• std::cout << "Hello, World!" — выводит текст на экран.
• std::endl — переводит строку.
• return 0 — сообщает системе, что программа завершилась успешно.

Попробуйте изменить текст в кавычках прямо в блоке выше и запустить программу ещё раз.`},
			{Type: "code", Language: "cpp", Caption: "std::cout умеет выводить и текст, и результаты вычислений", Content: `#include <iostream>

int main() {
    std::cout << "2 + 2 = " << 2 + 2 << std::endl;
    std::cout << "10 * 10 = " << 10 * 10 << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание для закрепления: выведите три строки — своё имя, свой город и результат вычисления 7 * 6. Каждое значение — на отдельной строке.

Когда получится — отметьте урок пройденным и переходите к следующему: «Переменные и типы данных».`},
		},
	}

	m["cpp-variables-types"] = lessonContentSeed{
		Summary: "Переменные, типы int, long long, double, char и bool, арифметика и целочисленное деление.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Программы работают с данными: числами, символами, текстом. Чтобы хранить данные, нужны переменные — именованные «ячейки памяти». В C++ у каждой переменной есть тип, который определяет, что в ней может лежать.

Главные типы, которые вам понадобятся:

• int — целые числа примерно до 2 миллиардов (2·10⁹);
• long long — большие целые до 9·10¹⁸, основной тип олимпиадника;
• double — дробные числа, например 3.14;
• char — один символ в одинарных кавычках, например 'A';
• bool — логическое значение: true (истина) или false (ложь).`},
			{Type: "code", Language: "cpp", Caption: "Объявление переменных разных типов", Content: `#include <iostream>

int main() {
    int age = 15;
    long long big = 1000000000000LL;
    double pi = 3.14159;
    char letter = 'A';
    bool ready = true;

    std::cout << age << "\n";
    std::cout << big << "\n";
    std::cout << pi << "\n";
    std::cout << letter << "\n";
    std::cout << ready << "\n";
    return 0;
}`},
			{Type: "text", Content: `Обратите внимание: bool печатается как 1 (true) или 0 (false).

С числами можно выполнять операции: сложение +, вычитание -, умножение *, деление / и остаток от деления %.

Важная особенность C++: деление двух целых чисел отбрасывает дробную часть. 7 / 2 равно 3, а не 3.5. Остаток от деления: 7 % 2 равно 1. Остаток — очень частый приём в задачах: например, число чётное, если n % 2 == 0.

Частая ошибка новичков — переполнение: если перемножить два int по миллиарду, результат не помещается в int и получается «мусор». Для больших значений всегда используйте long long.`},
			{Type: "code", Language: "cpp", Caption: "Арифметика, целочисленное деление и long long", Content: `#include <iostream>

int main() {
    int a = 7, b = 2;
    std::cout << a + b << " " << a - b << " " << a * b << "\n";
    std::cout << a / b << " <- целая часть от деления\n";
    std::cout << a % b << " <- остаток от деления\n";

    long long x = 1000000000;
    std::cout << x * x << " <- long long не переполнился\n";
    return 0;
}`},
			{Type: "text", Content: `Задание: заведите две переменные со значениями 15 и 4. Выведите их сумму, разность, произведение, целую часть от деления и остаток — каждое на отдельной строке.

Затем отметьте урок пройденным и переходите к чтению входных данных.`},
		},
	}

	m["cpp-input"] = lessonContentSeed{
		Summary:  "Чтение чисел и слов со стандартного ввода с помощью std::cin.",
		Problems: []string{"Сумма двух чисел"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `В олимпиадных задачах данные приходят со стандартного ввода: программа читает числа и строки, обрабатывает их и печатает ответ. Именно так вас проверяет автоматическая система: она подаёт программе тесты на вход и сравнивает вывод с правильным ответом.

Для чтения в C++ служит std::cin. Он сам пропускает пробелы и переводы строк, поэтому не важно, записаны числа в одну строку или в несколько.

Нажмите кнопку «Входные данные» над кодом, введите два числа через пробел (например, 3 5) и запустите программу.`},
			{Type: "code", Language: "cpp", Caption: "Введите в поле «Входные данные» два числа, например: 3 5", Content: `#include <iostream>

int main() {
    int a, b;
    std::cin >> a >> b;
    std::cout << a + b << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Оператор >> читает значения по очереди: сначала в a, потом в b. Так можно прочитать сколько угодно значений подряд.

Слова (без пробелов) читаются в тип std::string — для него нужно подключить библиотеку string.`},
			{Type: "code", Language: "cpp", Caption: "Введите имя и возраст, например: Азат 15", Content: `#include <iostream>
#include <string>

int main() {
    std::string name;
    int age;
    std::cin >> name >> age;
    std::cout << "Привет, " << name << "! Тебе " << age << " лет." << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание: прочитайте три числа и выведите их среднее арифметическое. Чтобы ответ получился дробным, делите на 3.0, а не на 3.

Главная практика — ниже: решите задачу «Сумма двух чисел». Откройте её, напишите решение и отправьте — система проверит его на тестах. Добейтесь вердикта Accepted, затем отметьте урок пройденным.`},
		},
	}

	m["cpp-conditions"] = lessonContentSeed{
		Summary:  "Оператор if/else, сравнения и логические связки && и ||.",
		Problems: []string{"Чётное или нечётное"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Программа становится полезной, когда умеет принимать решения: «если число чётное — напиши одно, иначе — другое». Для этого служит оператор if/else.

Операторы сравнения: == (равно), != (не равно), < , > , <= , >=.

Внимание на классическую ошибку: сравнение пишется двумя знаками равенства ==. Одиночное = — это присваивание, и внутри if оно почти всегда означает баг.`},
			{Type: "code", Language: "cpp", Caption: "Введите число — программа скажет, чётное ли оно", Content: `#include <iostream>

int main() {
    int n;
    std::cin >> n;

    if (n % 2 == 0) {
        std::cout << "EVEN" << std::endl;
    } else {
        std::cout << "ODD" << std::endl;
    }
    return 0;
}`},
			{Type: "text", Content: `Условия можно объединять:

• && — логическое И: оба условия должны быть истинны;
• || — логическое ИЛИ: достаточно одного;
• ! — отрицание.

Несколько веток разбираются цепочкой if / else if / else. Ниже — программа, которая находит наибольшее из трёх чисел.`},
			{Type: "code", Language: "cpp", Caption: "Введите три числа, например: 3 9 5", Content: `#include <iostream>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;

    if (a >= b && a >= c) {
        std::cout << a << std::endl;
    } else if (b >= a && b >= c) {
        std::cout << b << std::endl;
    } else {
        std::cout << c << std::endl;
    }
    return 0;
}`},
			{Type: "text", Content: `Задание: прочитайте возраст человека и выведите «school», если ему от 7 до 17 лет включительно, иначе — «other». Понадобится связка &&.

Затем решите прикреплённую задачу «Чётное или нечётное» и отметьте урок пройденным.`},
		},
	}

	m["cpp-loops"] = lessonContentSeed{
		Summary:  "Циклы for и while: повторение действий, счётчики и накопление результата.",
		Problems: []string{"Факториал числа", "Сумма элементов массива"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Циклы позволяют повторять действия много раз. В олимпиадных задачах почти всегда нужно перебрать числа, элементы массива или символы строки — всё это делается циклами.

Цикл for состоит из трёх частей: старт; условие продолжения; шаг. Классическая форма — счётчик от 1 до n.`},
			{Type: "code", Language: "cpp", Caption: "Счётчик от 1 до 5", Content: `#include <iostream>

int main() {
    for (int i = 1; i <= 5; i++) {
        std::cout << "Шаг номер " << i << "\n";
    }
    return 0;
}`},
			{Type: "text", Content: `Самый частый приём с циклом — накопление: заводим переменную-накопитель (сумму, произведение, счётчик) и обновляем её на каждом шаге.

Ниже программа читает n, затем n чисел, и считает их сумму. Заметьте: сумма объявлена как long long — если чисел много и они большие, int переполнится.`},
			{Type: "code", Language: "cpp", Caption: "Введите: 4, затем 10 20 30 40", Content: `#include <iostream>

int main() {
    int n;
    std::cin >> n;

    long long sum = 0;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        sum += x;
    }
    std::cout << sum << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Второй вид цикла — while: он повторяется, пока условие истинно, и удобен, когда число шагов заранее неизвестно.`},
			{Type: "code", Language: "cpp", Caption: "Сколько раз число делится на 2? Введите, например: 96", Content: `#include <iostream>

int main() {
    int n;
    std::cin >> n;

    int count = 0;
    while (n % 2 == 0) {
        n /= 2;
        count++;
    }
    std::cout << count << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание: выведите все чётные числа от 2 до 20 в одну строку через пробел.

Затем решите прикреплённые задачи «Факториал числа» (накопление произведения) и «Сумма элементов массива» (накопление суммы) — и отметьте урок пройденным.`},
		},
	}

	m["cpp-arrays"] = lessonContentSeed{
		Summary:  "std::vector — массив: чтение n элементов, обход и поиск максимума.",
		Problems: []string{"Максимум в массиве"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Массив хранит много значений одного типа под одним именем. В современном C++ вместо «сырых» массивов используют std::vector — массив, который сам знает свой размер и умеет расти.

Элементы нумеруются с нуля: в векторе из n элементов индексы идут от 0 до n-1. Выход за границы — одна из самых частых причин вердикта Runtime Error.`},
			{Type: "code", Language: "cpp", Caption: "Введите: 5, затем 1 3 5 2 4", Content: `#include <iostream>
#include <vector>

int main() {
    int n;
    std::cin >> n;

    std::vector<int> a(n);
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }

    for (int i = n - 1; i >= 0; i--) {
        std::cout << a[i] << " ";
    }
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Программа выше читает массив и печатает его в обратном порядке — обратите внимание на цикл с шагом назад.

Второй базовый приём — поиск максимума: берём первый элемент за «текущий максимум» и в цикле обновляем его, если нашли элемент больше.`},
			{Type: "code", Language: "cpp", Caption: "Поиск максимума. Введите: 5, затем 1 3 5 2 4", Content: `#include <iostream>
#include <vector>

int main() {
    int n;
    std::cin >> n;

    std::vector<int> a(n);
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }

    int best = a[0];
    for (int i = 1; i < n; i++) {
        if (a[i] > best) {
            best = a[i];
        }
    }
    std::cout << best << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание: по тому же принципу найдите минимум массива и выведите его вместе с максимумом через пробел.

Затем решите прикреплённую задачу «Максимум в массиве» и отметьте урок пройденным.`},
		},
	}

	m["cpp-strings"] = lessonContentSeed{
		Summary:  "std::string: длина, доступ к символам, перебор и проверка палиндрома.",
		Problems: []string{"Переворот строки", "Проверка палиндрома", "Подсчёт гласных"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Строка — это последовательность символов. В C++ строки хранятся в типе std::string, а обращаться к символам можно по индексу, как в массиве: s[0] — первый символ, s[s.size() - 1] — последний.

Метод s.size() возвращает длину строки. Строки можно сравнивать операторами == и <, складывать оператором + и перебирать циклом.`},
			{Type: "code", Language: "cpp", Caption: "Введите слово, например: hello", Content: `#include <iostream>
#include <string>

int main() {
    std::string s;
    std::cin >> s;

    std::cout << "Длина: " << s.size() << "\n";
    std::cout << "Первый символ: " << s[0] << "\n";

    for (int i = s.size() - 1; i >= 0; i--) {
        std::cout << s[i];
    }
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Программа выше печатает слово задом наперёд — перебором индексов с конца.

Классическая задача на строки — палиндром: читается ли слово одинаково слева направо и справа налево. Идея: сравниваем первый символ с последним, второй — с предпоследним, и так до середины.`},
			{Type: "code", Language: "cpp", Caption: "Проверка палиндрома. Введите: racecar", Content: `#include <iostream>
#include <string>

int main() {
    std::string s;
    std::cin >> s;

    bool ok = true;
    int n = s.size();
    for (int i = 0; i < n / 2; i++) {
        if (s[i] != s[n - 1 - i]) {
            ok = false;
        }
    }

    if (ok) std::cout << "YES" << std::endl;
    else std::cout << "NO" << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание: прочитайте слово и посчитайте, сколько в нём букв «a».

К уроку прикреплены три задачи: «Переворот строки», «Проверка палиндрома» и «Подсчёт гласных». Решите их все — это лучший способ закрепить работу со строками — и отметьте урок пройденным.`},
		},
	}

	m["cpp-functions"] = lessonContentSeed{
		Summary:  "Функции: параметры, возвращаемое значение и декомпозиция решения.",
		Problems: []string{"НОД двух чисел", "Проверка простого числа"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Функция — это именованный кусок кода, который можно вызывать сколько угодно раз. Функции делают решение читаемым: главная логика остаётся в main, а детали уходят в отдельные функции.

Функция объявляется так: тип результата, имя, параметры в скобках. Ключевое слово return возвращает результат и завершает функцию.`},
			{Type: "code", Language: "cpp", Caption: "Функция наибольшего общего делителя (алгоритм Евклида)", Content: `#include <iostream>

long long gcd(long long a, long long b) {
    while (b != 0) {
        long long r = a % b;
        a = b;
        b = r;
    }
    return a;
}

int main() {
    long long a, b;
    std::cin >> a >> b;
    std::cout << gcd(a, b) << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Это алгоритм Евклида — вычисление наибольшего общего делителя (НОД). Обратите внимание: функция объявлена выше main, иначе компилятор её «не увидит».

Функции могут возвращать bool — так удобно оформлять проверки. Ниже функция определяет, простое ли число: делители достаточно проверять только до корня из n, поэтому цикл идёт, пока i * i <= n. Это уже настоящая алгоритмическая оптимизация: миллион проверок вместо триллиона.`},
			{Type: "code", Language: "cpp", Caption: "Проверка простоты за корень. Введите, например: 97", Content: `#include <iostream>

bool isPrime(long long n) {
    if (n < 2) return false;
    for (long long i = 2; i * i <= n; i++) {
        if (n % i == 0) return false;
    }
    return true;
}

int main() {
    long long n;
    std::cin >> n;
    if (isPrime(n)) std::cout << "YES" << std::endl;
    else std::cout << "NO" << std::endl;
    return 0;
}`},
			{Type: "text", Content: `Задание: напишите функцию int digitSum(long long n), которая возвращает сумму цифр числа (подсказка: цифры удобно снимать через n % 10 и n /= 10).

Затем решите прикреплённые задачи «НОД двух чисел» и «Проверка простого числа» — и отметьте урок пройденным.`},
		},
	}

	m["cpp-stl"] = lessonContentSeed{
		Summary: "STL: сортировка sort, пары, set и map — инструменты, экономящие часы.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `STL (стандартная библиотека шаблонов) — главное преимущество C++ на олимпиадах. Готовые структуры данных и алгоритмы покрывают половину типовых подзадач.

Самое используемое — сортировка: std::sort из библиотеки algorithm сортирует вектор за O(n log n). Одной строкой.`},
			{Type: "code", Language: "cpp", Caption: "Сортировка. Введите: 5, затем 3 1 4 1 5", Content: `#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n;
    std::cin >> n;
    std::vector<int> a(n);
    for (int i = 0; i < n; i++) std::cin >> a[i];

    std::sort(a.begin(), a.end());

    for (int x : a) std::cout << x << " ";
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Обратите внимание на цикл for (int x : a) — это «range-for», короткий способ пройтись по всем элементам.

Ещё два незаменимых контейнера:

• std::set — хранит элементы без повторов и в отсортированном порядке; проверка «есть ли элемент» работает за O(log n);
• std::map — словарь «ключ → значение»; идеален для подсчёта, сколько раз встречается каждое слово или число.`},
			{Type: "code", Language: "cpp", Caption: "Подсчёт повторов через map. Введите: 6, затем 1 2 2 3 3 3", Content: `#include <iostream>
#include <map>

int main() {
    int n;
    std::cin >> n;

    std::map<int, int> count;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        count[x]++;
    }

    for (auto p : count) {
        std::cout << p.first << " встречается " << p.second << " раз\n";
    }
    return 0;
}`},
			{Type: "text", Content: `Задание: прочитайте n чисел и выведите количество различных среди них (подсказка: сложите всё в set и выведите его размер).

Отметьте урок пройденным — остался последний шаг курса: олимпиадный шаблон.`},
		},
	}

	m["cpp-competitive"] = lessonContentSeed{
		Summary: "Быстрый ввод-вывод, шаблон решения и checklist олимпиадника на C++.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Вы знаете достаточно C++, чтобы начинать олимпиадный путь. Последний штрих — привычки, которые уберегут от обидных вердиктов.

1. Быстрый ввод-вывод. По умолчанию cin/cout синхронизируются с языком C и работают медленно. Две строки в начале main ускоряют их в разы — на больших входных данных это разница между Accepted и Time Limit Exceeded.

2. Перевод строки. Используйте "\n" вместо std::endl: endl каждый раз принудительно сбрасывает буфер вывода и заметно тормозит программу.`},
			{Type: "code", Language: "cpp", Caption: "Шаблон, с которого стоит начинать каждое решение", Content: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    long long sum = 0;
    for (int i = 0; i < n; i++) {
        long long x;
        cin >> x;
        sum += x;
    }
    cout << sum << "\n";
    return 0;
}`},
			{Type: "text", Content: `Что здесь нового:

• #include <bits/stdc++.h> — подключает сразу всю стандартную библиотеку (работает в GCC, которым проверяет этот сайт);
• using namespace std — позволяет писать cin вместо std::cin;
• ios_base::sync_with_stdio(false) и cin.tie(nullptr) — тот самый быстрый ввод-вывод.

Checklist перед отправкой решения:

1. Помещается ли ответ в int? Сомневаетесь — берите long long.
2. Правильно ли обрабатываются крайние случаи: n = 1, нули, отрицательные числа?
3. Укладывается ли алгоритм в лимит времени? Ориентир: около 10⁸ простых операций в секунду.
4. Вывод точно совпадает с форматом из условия?`},
			{Type: "text", Content: `Курс завершён — поздравляем! Теперь вы готовы к систематической тренировке.

Откройте раздел «Обучение» и начинайте «Путь олимпиадника» с Уровня 1: сложность алгоритмов, задачи на реализацию и базовая математика. Отмечайте пройденные уроки — и удачи на соревнованиях!`},
		},
	}
}
