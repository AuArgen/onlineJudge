package database

// addRoadmapLevel12Content seeds olympiad roadmap lessons for levels 1-2:
// foundations (complexity, implementation, basic math) and sorting/searching
// techniques. Examples are in C++ — the de-facto contest standard — with
// Python variants where they help beginners bridge over.
func addRoadmapLevel12Content(m map[string]lessonContentSeed) {
	// ── Level 1. Foundations ────────────────────────────────────────────────

	m["complexity-big-o"] = lessonContentSeed{
		Summary:  "Как оценить скорость алгоритма до запуска: O-нотация и правило 10⁸ операций в секунду.",
		Problems: []string{"Сумма элементов массива"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Правильный ответ — только половина олимпиадной задачи. Вторая половина — уложиться в лимит времени, обычно 1–2 секунды. Поэтому первое, чему учится олимпиадник, — оценивать скорость алгоритма ДО того, как писать код.

Скорость измеряют количеством операций в зависимости от размера входа n и записывают через O-нотацию (читается «о большое»):

• O(1) — константа: ответ по формуле;
• O(log n) — деление задачи пополам: бинарный поиск;
• O(n) — один проход по данным;
• O(n log n) — сортировка;
• O(n²) — два вложенных цикла;
• O(2ⁿ) — полный перебор подмножеств.

Главное практическое правило: компьютер выполняет порядка 10⁸ простых операций в секунду. Подставьте свой n в формулу сложности — и вы знаете, пройдёт решение или нет.`},
			{Type: "code", Language: "cpp", Caption: "Сколько операций потребует каждый алгоритм? Введите n, например: 100000", Content: `#include <iostream>
#include <cmath>

int main() {
    long long n;
    std::cin >> n; // введите n до 10^9

    std::cout << "O(log n): ~" << (long long)std::log2((double)n) << " операций\n";
    std::cout << "O(n): ~" << n << "\n";
    std::cout << "O(n log n): ~" << (long long)(n * std::log2((double)n)) << "\n";
    std::cout << "O(n^2): ~" << n * n << "\n";
    if (n <= 60) std::cout << "O(2^n): ~" << (1LL << n) << "\n";
    else std::cout << "O(2^n): больше, чем атомов во Вселенной\n";
    return 0;
}`},
			{Type: "text", Content: `Попробуйте ввести 1000, потом 100000, потом 1000000000 — и сравните строки с правилом 10⁸. Видно, почему решение за O(n²) при n = 10⁵ уже не проходит (10¹⁰ операций — сто секунд), а за O(n log n) — легко.

Почувствуйте разницу вживую: программа ниже считает сумму чисел от 1 до n двумя способами — циклом за O(n) и формулой за O(1). Введите 1000000000 (миллиард) и посмотрите на время выполнения: цикл займёт заметное время, формула — мгновение. Ответы совпадут.`},
			{Type: "code", Language: "cpp", Caption: "Введите 1000000000 и сравните: цикл заметно думает, формула мгновенна", Content: `#include <iostream>

int main() {
    long long n;
    std::cin >> n;

    long long slow = 0;
    for (long long i = 1; i <= n; i++) slow += i;   // O(n)

    long long fast = n * (n + 1) / 2;               // O(1)

    std::cout << slow << "\n" << fast << "\n";
    return 0;
}`},
			{Type: "text", Content: `Как оценивать свой код:

• вложенные циклы перемножаются: цикл по n внутри цикла по n — это O(n²);
• последовательные блоки складываются, и берётся наибольший: O(n) + O(n log n) = O(n log n);
• константы отбрасываются: 5n операций — это всё равно O(n).

Задание: оцените сложность трёх фрагментов — (1) поиск максимума одним проходом; (2) проверка всех пар элементов; (3) цикл, в котором n каждый раз делится на 2. Ответы: O(n), O(n²), O(log n).

Решите прикреплённую задачу «Сумма элементов массива» — классический один проход за O(n) — и отметьте урок пройденным.`},
		},
	}

	m["implementation-problems"] = lessonContentSeed{
		Summary:  "Задачи на реализацию: читаем условие внимательно, обрабатываем краевые случаи, не теряем баллы на мелочах.",
		Problems: []string{"Чётное или нечётное", "Переворот строки", "Подсчёт гласных", "Максимум в массиве"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Задачи на реализацию не требуют хитрых алгоритмов — нужно аккуратно сделать ровно то, что написано в условии. Звучит просто, но именно на них новички теряют больше всего попыток.

Три главных врага:

1. Невнимательное чтение условия: перепутали строки и столбцы, забыли, что нумерация с единицы, не заметили слово «включительно».
2. Краевые случаи: n = 1, пустая строка, все элементы одинаковые, отрицательные числа.
3. Формат вывода: лишний пробел, недостающий перевод строки, YES вместо Yes.

Пример: нарисуем шахматную доску n на m из символов # и точек. Вся задача — заметить закономерность: цвет клетки определяется чётностью суммы координат.`},
			{Type: "code", Language: "cpp", Caption: "Шахматная доска. Введите: 3 5", Content: `#include <iostream>

int main() {
    int n, m;
    std::cin >> n >> m;

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            if ((i + j) % 2 == 0) std::cout << '#';
            else std::cout << '.';
        }
        std::cout << "\n";
    }
    return 0;
}`},
			{Type: "text", Content: `Второй пример потоньше: найти ВТОРОЙ по величине элемент массива. Наивное «отсортировать и взять предпоследний» ломается, если максимум встречается дважды, а решение с двумя переменными требует аккуратной инициализации — классическая ловушка реализации.`},
			{Type: "code", Language: "cpp", Caption: "Второй максимум. Введите: 5, затем 3 9 4 9 7", Content: `#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n; // n >= 2
    std::cin >> n;
    std::vector<long long> a(n);
    for (auto &x : a) std::cin >> x;

    long long best = std::max(a[0], a[1]);
    long long second = std::min(a[0], a[1]);
    for (int i = 2; i < n; i++) {
        if (a[i] > best) {
            second = best;
            best = a[i];
        } else if (a[i] > second) {
            second = a[i];
        }
    }
    std::cout << second << "\n";
    return 0;
}`},
			{Type: "text", Content: `Checklist перед отправкой любой задачи:

1. Перечитайте условие ещё раз ПОСЛЕ написания кода — вы удивитесь, как часто решается «не та задача».
2. Прогоните пример из условия руками.
3. Придумайте свои краевые тесты: минимальный n, одинаковые элементы, крайние значения из ограничений.
4. Проверьте типы: помещается ли ответ в int?

К уроку прикреплены четыре задачи на аккуратность. Решите все — и отметьте урок пройденным.`},
		},
	}

	m["basic-math"] = lessonContentSeed{
		Summary:  "Делимость, НОД и НОК, простые числа, решето Эратосфена и разложение на множители.",
		Problems: []string{"НОД двух чисел", "Проверка простого числа", "Факториал числа"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Числовая база олимпиадника — четыре темы.

1. Делимость и остатки. a делится на b, если a % b == 0. Остатки зациклены: последняя цифра числа — это n % 10, чётность — n % 2.

2. НОД и НОК. Наибольший общий делитель считается алгоритмом Евклида за O(log n) (вы писали его в курсе языка). Наименьшее общее кратное: НОК(a,b) = a / НОД(a,b) * b — именно в таком порядке, чтобы не переполниться.

3. Простые числа. Проверка одного числа — перебор делителей до корня. А если нужны ВСЕ простые до n — решето Эратосфена: выписываем числа и вычёркиваем кратные каждому простому.`},
			{Type: "code", Language: "cpp", Caption: "Решето Эратосфена: все простые до n. Введите: 50", Content: `#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n;
    std::cin >> n;

    std::vector<bool> isPrime(std::max(n + 1, 2), true);
    isPrime[0] = isPrime[1] = false;
    for (long long i = 2; i * i <= n; i++) {
        if (isPrime[i]) {
            for (long long j = i * i; j <= n; j += i) {
                isPrime[j] = false;
            }
        }
    }

    for (int i = 2; i <= n; i++) {
        if (isPrime[i]) std::cout << i << " ";
    }
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Почему решето быстрое: каждое составное число вычёркивается своими простыми делителями, суммарно получается O(n log log n) — почти линейно. Для n = 10⁷ работает меньше секунды.

Тонкость: вычёркивание начинается с i * i, а не с 2 * i — все меньшие кратные уже вычеркнуты меньшими простыми.

4. Разложение на простые множители: делим число на все делители до корня; если что-то осталось больше единицы — это последний простой множитель.`},
			{Type: "code", Language: "cpp", Caption: "Разложение на простые множители. Введите: 360", Content: `#include <iostream>

int main() {
    long long n;
    std::cin >> n;

    for (long long d = 2; d * d <= n; d++) {
        while (n % d == 0) {
            std::cout << d << " ";
            n /= d;
        }
    }
    if (n > 1) std::cout << n;
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Задание: с помощью решета посчитайте, сколько простых чисел меньше миллиона (ответ: 78498 — проверьте себя).

Затем решите прикреплённые задачи «НОД двух чисел», «Проверка простого числа» и «Факториал числа» — и отметьте урок пройденным.`},
		},
	}

	// ── Level 2. Sorting and searching ─────────────────────────────────────

	m["sorting"] = lessonContentSeed{
		Summary: "Сортировка как инструмент: std::sort, компараторы, сортировка структур.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Сортировка — самый используемый алгоритм в олимпиадах, но не как самоцель, а как инструмент: после сортировки задача часто упрощается радикально. Ближайшая пара чисел? Отсортируйте — и она окажется среди соседей. Жадный выбор? Почти всегда начинается со «отсортируем по...».

Писать сортировку руками не нужно: std::sort работает за O(n log n). Нужно уметь ей управлять — задавать компаратор, то есть правило сравнения.`},
			{Type: "code", Language: "cpp", Caption: "Сортировка по возрастанию и по убыванию. Введите: 5, затем 3 1 4 1 5", Content: `#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n;
    std::cin >> n;
    std::vector<int> a(n);
    for (auto &x : a) std::cin >> x;

    std::sort(a.begin(), a.end());
    for (int x : a) std::cout << x << " ";
    std::cout << "\n";

    std::sort(a.begin(), a.end(), [](int x, int y) { return x > y; });
    for (int x : a) std::cout << x << " ";
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Компаратор — функция «должен ли x стоять раньше y». Лямбда [](int x, int y) { return x > y; } даёт убывание.

Настоящая сила — сортировка структур по любому полю. Отсортируем учеников по баллам: правило сравнения читается прямо из кода.`},
			{Type: "code", Language: "cpp", Caption: "Введите: 3, затем строки вида «имя балл»: Azat 90, Aigul 98, Bek 85", Content: `#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

struct Student {
    std::string name;
    int score;
};

int main() {
    int n;
    std::cin >> n;
    std::vector<Student> a(n);
    for (auto &s : a) std::cin >> s.name >> s.score;

    std::sort(a.begin(), a.end(), [](const Student &x, const Student &y) {
        return x.score > y.score; // по убыванию баллов
    });

    for (auto &s : a) std::cout << s.name << " " << s.score << "\n";
    return 0;
}`},
			{Type: "code", Language: "python", Caption: "То же самое на Python: параметр key. Тот же ввод", Content: `n = int(input())
students = []
for _ in range(n):
    name, score = input().split()
    students.append((name, int(score)))

students.sort(key=lambda s: -s[1])

for name, score in students:
    print(name, score)`},
			{Type: "text", Content: `Полезно знать: std::stable_sort сохраняет порядок равных элементов (важно при сортировке «по баллам, а при равенстве — по имени» в два прохода).

Задание: отсортируйте список слов сначала по длине, а при равной длине — по алфавиту (подсказка: компаратор с двумя условиями).

Отметьте урок пройденным и переходите к методу двух указателей.`},
		},
	}

	m["two-pointers"] = lessonContentSeed{
		Summary:  "Метод двух указателей: пары с заданной суммой и скользящее окно за O(n).",
		Problems: []string{"Проверка палиндрома"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Идея метода: два индекса ходят по массиву только вперёд (или навстречу друг другу) и никогда не возвращаются. Каждый указатель сдвигается не больше n раз — итого O(n) вместо O(n²) перебора всех пар.

Классика номер один: в ОТСОРТИРОВАННОМ массиве найти пару с суммой X. Ставим указатели на края: если сумма мала — двигаем левый вправо (сумма вырастет), если велика — правый влево.`},
			{Type: "code", Language: "cpp", Caption: "Пара с суммой X. Введите: 5 12, затем отсортированный массив 1 3 5 7 9", Content: `#include <iostream>
#include <vector>

int main() {
    int n;
    long long target;
    std::cin >> n >> target;
    std::vector<long long> a(n);
    for (auto &x : a) std::cin >> x; // массив отсортирован

    int l = 0, r = n - 1;
    while (l < r) {
        long long sum = a[l] + a[r];
        if (sum == target) {
            std::cout << a[l] << " + " << a[r] << " = " << target << "\n";
            return 0;
        }
        if (sum < target) l++;
        else r--;
    }
    std::cout << "Пары нет\n";
    return 0;
}`},
			{Type: "text", Content: `Почему это верно: если a[l] + a[r] < X, то a[l] в паре с ЛЮБЫМ элементом левее r даст ещё меньше — значит, a[l] можно смело исключить. Симметрично для правого. Ни одна возможная пара не теряется.

Классика номер два — скользящее окно: оба указателя идут в одну сторону. Найдём самый длинный отрезок, сумма которого не превышает S: правый край расширяет окно, левый поджимается, когда сумма превысила лимит.`},
			{Type: "code", Language: "cpp", Caption: "Самый длинный отрезок с суммой не больше S. Введите: 6 8, затем 2 4 1 3 5 2", Content: `#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n;
    long long s;
    std::cin >> n >> s;
    std::vector<long long> a(n);
    for (auto &x : a) std::cin >> x;

    long long sum = 0;
    int best = 0, l = 0;
    for (int r = 0; r < n; r++) {
        sum += a[r];
        while (sum > s) {
            sum -= a[l];
            l++;
        }
        best = std::max(best, r - l + 1);
    }
    std::cout << best << "\n";
    return 0;
}`},
			{Type: "text", Content: `Обратите внимание: внутренний while не делает алгоритм квадратичным — левый указатель за всю работу программы сдвинется суммарно не больше n раз.

Проверка палиндрома, которую вы писали в курсе языка, — тоже два указателя, идущие навстречу. Решите прикреплённую задачу «Проверка палиндрома», осознав её теперь как частный случай метода, и отметьте урок пройденным.`},
		},
	}

	m["prefix-sums"] = lessonContentSeed{
		Summary:  "Префиксные суммы: ответ на запрос «сумма на отрезке» за O(1); разностный массив.",
		Problems: []string{"Сумма элементов массива"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Ситуация: массив из n чисел и q запросов «чему равна сумма с l-го по r-й элемент?». Считать каждый запрос циклом — O(n·q): при n = q = 10⁵ это 10¹⁰ операций, безнадёжно.

Решение — предподсчёт. Заведём массив префиксных сумм: p[i] = сумма первых i элементов. Тогда сумма на отрезке [l, r] — это просто p[r] - p[l-1]: из «суммы до r» вычли «сумму до l-1». Каждый запрос — O(1), всё вместе — O(n + q).`},
			{Type: "code", Language: "cpp", Caption: "Введите: 5 2, массив 1 2 3 4 5, затем запросы: 2 4 и 1 5", Content: `#include <iostream>
#include <vector>

int main() {
    int n, q;
    std::cin >> n >> q;

    std::vector<long long> p(n + 1, 0);
    for (int i = 1; i <= n; i++) {
        long long x;
        std::cin >> x;
        p[i] = p[i - 1] + x;
    }

    while (q--) {
        int l, r; // границы с единицы
        std::cin >> l >> r;
        std::cout << p[r] - p[l - 1] << "\n";
    }
    return 0;
}`},
			{Type: "text", Content: `Детали, которые стоит запомнить:

• индексация с единицы и p[0] = 0 избавляют от особого случая l = 1;
• префиксные суммы копятся в long long — сумма ста тысяч миллиардов в int не влезет;
• та же идея работает в 2D: préfix-таблица позволяет за O(1) отвечать на сумму в прямоугольнике.

Зеркальный приём — разностный массив: если запросы наоборот ИЗМЕНЯЮТ отрезки («прибавить x всем от l до r»), а ответ нужен один раз в конце, храните разности: d[l] += x, d[r+1] -= x, а в конце восстановите массив префиксными суммами самих разностей.`},
			{Type: "code", Language: "cpp", Caption: "Разностный массив. Введите: 5 2, затем запросы «1 3 2» и «2 5 1»", Content: `#include <iostream>
#include <vector>

int main() {
    int n, q;
    std::cin >> n >> q;
    std::vector<long long> d(n + 2, 0);

    while (q--) {
        int l, r;
        long long x; // прибавить x на отрезке [l, r]
        std::cin >> l >> r >> x;
        d[l] += x;
        d[r + 1] -= x;
    }

    long long cur = 0;
    for (int i = 1; i <= n; i++) {
        cur += d[i];
        std::cout << cur << " ";
    }
    std::cout << "\n";
    return 0;
}`},
			{Type: "text", Content: `Задание: по массиву префиксных сумм найдите количество отрезков с нулевой суммой (подсказка: сумма [l, r] нулевая, когда p[r] == p[l-1]; посчитайте одинаковые префиксы через map — это связка двух тем!).

Отметьте урок пройденным и переходите к бинарному поиску.`},
		},
	}

	m["binary-search"] = lessonContentSeed{
		Summary: "Бинарный поиск по массиву и по ответу: O(log n), инварианты и типичные ошибки.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Если данные отсортированы (или условие монотонно: «до какого-то места — нет, дальше — да»), искать можно делением пополам: каждый шаг отбрасывает половину вариантов. Миллиард элементов — всего 30 шагов. Это и есть бинарный поиск, O(log n).

Писать его нужно аккуратно: ошибки «на единицу» и вечные циклы — визитная карточка темы. Надёжный шаблон — полуинтервал [l, r): ищем первый индекс, где условие выполняется.`},
			{Type: "code", Language: "cpp", Caption: "Первое вхождение x. Введите: 6 5, затем отсортированный массив 1 3 5 5 7 9", Content: `#include <iostream>
#include <vector>

int main() {
    int n;
    long long x;
    std::cin >> n >> x;
    std::vector<long long> a(n);
    for (auto &v : a) std::cin >> v; // массив отсортирован

    int l = 0, r = n; // ответ в [l, r): первый индекс, где a[i] >= x
    while (l < r) {
        int mid = (l + r) / 2;
        if (a[mid] >= x) r = mid;
        else l = mid + 1;
    }

    if (l < n && a[l] == x) std::cout << "Первое вхождение: позиция " << l + 1 << "\n";
    else std::cout << "Не найден\n";
    return 0;
}`},
			{Type: "text", Content: `В STL это уже есть: std::lower_bound(a.begin(), a.end(), x) возвращает итератор на первый элемент не меньше x, upper_bound — строго больше. Но писать поиск руками вы обязаны уметь — из-за главного олимпиадного приёма ниже.

Бинарный поиск ПО ОТВЕТУ. Часто сам ответ монотонен: «можно ли уложиться за время t?» — если можно за t, можно и за t+1. Тогда бинарным поиском ищется граница между «нельзя» и «можно», а вам остаётся написать только проверку. Пример: целочисленный квадратный корень — наибольшее m, такое что m² не превышает n.`},
			{Type: "code", Language: "cpp", Caption: "Целочисленный корень бинпоиском по ответу. Введите: 1000000000000", Content: `#include <iostream>

int main() {
    long long n;
    std::cin >> n;

    long long l = 0, r = 2000000000; // ответ где-то в [l, r]
    while (l < r) {
        long long mid = (l + r + 1) / 2; // округление вверх!
        if (mid * mid <= n) l = mid;     // mid подходит - идём вправо
        else r = mid - 1;
    }
    std::cout << l << "\n";
    return 0;
}`},
			{Type: "text", Content: `Три классические ошибки:

1. Вечный цикл: если ищете ПОСЛЕДНЕЕ подходящее (l = mid), середину надо округлять вверх: (l + r + 1) / 2. Иначе при r - l = 1 цикл зависнет.
2. Переполнение середины: в других языках пишут l + (r - l) / 2; в C++ с long long обычно безопасно, но помните об этом.
3. Неверные границы: ответ обязан лежать в стартовом [l, r] — проверяйте крайние значения.

Задание: дано k станков, каждый печатает лист за t секунд, и нужно n листов. За какое минимальное время они справятся? Решите бинпоиском по ответу с проверкой «сколько листов успеем за время T».

Отметьте урок пройденным — Уровень 2 завершён!`},
		},
	}
}
