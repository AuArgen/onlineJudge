package database

// addPythonCourseContent seeds the "Python from scratch" course lessons.
func addPythonCourseContent(m map[string]lessonContentSeed) {
	m["py-first-program"] = lessonContentSeed{
		Summary: "Первая программа на Python: print, вывод текста и вычислений.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Python — самый дружелюбный язык для старта: никакой лишней «обвязки», первая программа занимает одну строку.

Функция print выводит на экран всё, что ей передали. Нажмите «Запустить» и посмотрите на результат.`},
			{Type: "code", Language: "python", Caption: "Ваша первая программа на Python", Content: `print("Hello, World!")`},
			{Type: "text", Content: `print умеет выводить не только текст, но и результаты вычислений, причём несколько значений сразу — через запятую (между ними автоматически появится пробел).

Текст пишется в кавычках, вычисления — без кавычек. Сравните: print("2 + 2") напечатает буквально «2 + 2», а print(2 + 2) — число 4.`},
			{Type: "code", Language: "python", Caption: "Текст и вычисления в одном print", Content: `print("2 + 2 =", 2 + 2)
print("10 * 10 =", 10 * 10)
print("Стоимость:", 3 * 250, "сом")`},
			{Type: "text", Content: `Задание: выведите три строки — своё имя, свой город и результат вычисления 7 * 6. Каждое значение — отдельным print.

Когда получится — отметьте урок пройденным и переходите к переменным.`},
		},
	}

	m["py-variables-types"] = lessonContentSeed{
		Summary: "Переменные и типы int, float, str, bool; арифметика и особенности деления.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Переменная — это имя, за которым закреплено значение. В Python тип указывать не нужно: язык сам понимает его по значению.

Основные типы:

• int — целые числа. Приятный бонус Python: они не переполняются, можно считать хоть 100-значные числа;
• float — дробные числа, например 3.14;
• str — строка, текст в кавычках;
• bool — True или False.`},
			{Type: "code", Language: "python", Caption: "Переменные и функция type", Content: `age = 15
pi = 3.14159
name = "Азат"
ready = True

print(age, type(age))
print(pi, type(pi))
print(name, type(name))
print(ready, type(ready))`},
			{Type: "text", Content: `Арифметика: + - * , а с делением в Python есть важная тонкость:

• / — обычное деление, результат всегда дробный: 7 / 2 равно 3.5;
• // — целочисленное деление: 7 // 2 равно 3;
• % — остаток от деления: 7 % 2 равно 1;
• ** — возведение в степень: 2 ** 10 равно 1024.

В олимпиадных задачах чаще нужны именно // и %: помните про разницу между / и //, это классическая ошибка.`},
			{Type: "code", Language: "python", Caption: "Виды деления и большие числа", Content: `a = 7
b = 2
print(a / b)   # 3.5 - дробное
print(a // b)  # 3 - целая часть
print(a % b)   # 1 - остаток
print(2 ** 100)  # int в Python не переполняется`},
			{Type: "text", Content: `Задание: заведите переменные со значениями 15 и 4 и выведите их сумму, разность, произведение, целую часть от деления и остаток — каждое на отдельной строке.

Затем отметьте урок пройденным.`},
		},
	}

	m["py-input"] = lessonContentSeed{
		Summary:  "Чтение входных данных: input, int(input()) и map для нескольких чисел в строке.",
		Problems: []string{"Сумма двух чисел"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `В задачах данные приходят со стандартного ввода. В Python строку читает функция input().

Важно: input() всегда возвращает строку. Чтобы получить число, строку надо преобразовать: int(input()) — целое, float(input()) — дробное. Забыть про int — самая частая ошибка новичка: программа склеит «3» и «5» в «35» вместо того, чтобы сложить 3 + 5.

Нажмите «Входные данные» над кодом, введите число и запустите.`},
			{Type: "code", Language: "python", Caption: "Введите в поле «Входные данные» число, например: 7", Content: `n = int(input())
print("Квадрат числа:", n * n)`},
			{Type: "text", Content: `Если в одной строке несколько чисел через пробел, используется связка split и map:

• input().split() — разбивает строку на части по пробелам;
• map(int, ...) — превращает каждую часть в число.

Эту строку вы будете писать в каждой второй задаче — запомните её как формулу.`},
			{Type: "code", Language: "python", Caption: "Введите два числа через пробел, например: 3 5", Content: `a, b = map(int, input().split())
print(a + b)`},
			{Type: "text", Content: `Задание: прочитайте три числа из одной строки и выведите их среднее арифметическое.

Главная практика — прикреплённая задача «Сумма двух чисел»: решите её, добейтесь вердикта Accepted и отметьте урок пройденным.`},
		},
	}

	m["py-conditions"] = lessonContentSeed{
		Summary:  "Условия if/elif/else, отступы как часть синтаксиса, связки and/or/not.",
		Problems: []string{"Чётное или нечётное"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Оператор if выполняет код только при истинном условии. Особенность Python: тело условия выделяется отступом (4 пробела) — отступы здесь часть синтаксиса, а не оформление.

Сравнения: == (равно), != (не равно), < > <= >=. Равенство — два знака ==, одиночное = означает присваивание.`},
			{Type: "code", Language: "python", Caption: "Введите число — программа скажет, чётное ли оно", Content: `n = int(input())

if n % 2 == 0:
    print("EVEN")
else:
    print("ODD")`},
			{Type: "text", Content: `Несколько веток записываются через elif («иначе если»). Условия объединяются словами and, or и not — Python читается почти как английский.

Удобная особенность: двойные неравенства работают напрямую, можно писать 7 <= age <= 17.`},
			{Type: "code", Language: "python", Caption: "Введите три числа, например: 3 9 5", Content: `a, b, c = map(int, input().split())

if a >= b and a >= c:
    print(a)
elif b >= c:
    print(b)
else:
    print(c)`},
			{Type: "text", Content: `Задание: прочитайте возраст и выведите «school», если он от 7 до 17 включительно, иначе «other». Попробуйте оба варианта записи условия: со связкой and и двойным неравенством.

Затем решите прикреплённую задачу «Чётное или нечётное» и отметьте урок пройденным.`},
		},
	}

	m["py-loops"] = lessonContentSeed{
		Summary:  "Циклы for range и while: перебор, накопители и типовые приёмы.",
		Problems: []string{"Факториал числа", "Сумма элементов массива"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Цикл for в Python перебирает элементы последовательности. Для перебора чисел служит range:

• range(5) — числа 0, 1, 2, 3, 4 (пять чисел, с нуля!);
• range(1, 6) — числа 1, 2, 3, 4, 5 (правая граница не включается);
• range(10, 0, -2) — 10, 8, 6, 4, 2 (третий аргумент — шаг).

То, что правая граница не входит в range — источник вечных ошибок «на единицу». Проверяйте границы на маленьком примере.`},
			{Type: "code", Language: "python", Caption: "Счётчик от 1 до 5", Content: `for i in range(1, 6):
    print("Шаг номер", i)`},
			{Type: "text", Content: `Главный приём с циклами — накопление: заводим переменную и обновляем её на каждом шаге. Ниже — чтение n чисел и подсчёт их суммы.`},
			{Type: "code", Language: "python", Caption: "Введите: 4, затем строку 10 20 30 40", Content: `n = int(input())
nums = list(map(int, input().split()))

total = 0
for x in nums:
    total += x
print(total)`},
			{Type: "text", Content: `Цикл while повторяется, пока условие истинно, — он нужен, когда число шагов заранее неизвестно.`},
			{Type: "code", Language: "python", Caption: "Сколько раз число делится на 2? Введите: 96", Content: `n = int(input())

count = 0
while n % 2 == 0:
    n //= 2
    count += 1
print(count)`},
			{Type: "text", Content: `Задание: выведите все чётные числа от 2 до 20 в одну строку (подсказка: у print есть параметр end, print(x, end=" ") не переводит строку).

Затем решите прикреплённые задачи «Факториал числа» и «Сумма элементов массива» — и отметьте урок пройденным.`},
		},
	}

	m["py-lists"] = lessonContentSeed{
		Summary:  "Списки: индексы, срезы, встроенные функции len, sum, max, min и sort.",
		Problems: []string{"Максимум в массиве"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Список (list) — главная структура данных Python: упорядоченный набор значений. Индексы начинаются с нуля; отрицательные индексы считают с конца: a[-1] — последний элемент.

Срезы — фирменный приём Python: a[1:4] — элементы с индексами 1, 2, 3; a[::-1] — список в обратном порядке.`},
			{Type: "code", Language: "python", Caption: "Индексы, срезы и добавление элементов", Content: `a = [10, 20, 30, 40, 50]

print(a[0], a[-1])   # первый и последний
print(a[1:4])        # срез: [20, 30, 40]
print(a[::-1])       # весь список задом наперёд

a.append(60)         # добавить в конец
print(a, "длина:", len(a))`},
			{Type: "text", Content: `Для списков есть готовые функции, которые в C++ пришлось бы писать циклом: len (длина), sum (сумма), max и min, sorted (отсортированная копия), метод a.sort() (сортировка на месте).

Не злоупотребляйте ими бездумно: на собеседовании или разборе вас могут попросить написать поиск максимума руками — поэтому ниже оба варианта.`},
			{Type: "code", Language: "python", Caption: "Максимум: встроенной функцией и вручную. Введите: 5, затем 1 3 5 2 4", Content: `n = int(input())
a = list(map(int, input().split()))

print(max(a))  # способ 1: встроенная функция

best = a[0]    # способ 2: вручную, как это устроено внутри
for x in a:
    if x > best:
        best = x
print(best)`},
			{Type: "text", Content: `Задание: прочитайте список чисел и выведите его отсортированным по убыванию (подсказка: sorted(a, reverse=True)).

Затем решите прикреплённую задачу «Максимум в массиве» и отметьте урок пройденным.`},
		},
	}

	m["py-strings"] = lessonContentSeed{
		Summary:  "Строки: индексы, срезы, методы lower/upper/count и проверка палиндрома.",
		Problems: []string{"Переворот строки", "Проверка палиндрома", "Подсчёт гласных"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Строка в Python очень похожа на список символов: к ней применимы индексы, срезы и len. Перевернуть строку — один срез: s[::-1].

Полезные методы: s.lower() и s.upper() — регистр, s.count(x) — сколько раз встречается подстрока, x in s — проверка «содержится ли».

Важно: строки неизменяемы — s[0] = "a" вызовет ошибку. Изменённая строка — это всегда новая строка.`},
			{Type: "code", Language: "python", Caption: "Введите слово, например: hello", Content: `s = input()

print("Длина:", len(s))
print("Первый символ:", s[0])
print("Задом наперёд:", s[::-1])
print("Букв l:", s.count("l"))`},
			{Type: "text", Content: `Палиндром — слово, которое читается одинаково в обе стороны. В Python проверка занимает одну строку: сравнить строку с её перевёрнутой копией.`},
			{Type: "code", Language: "python", Caption: "Проверка палиндрома. Введите: racecar", Content: `s = input()

if s == s[::-1]:
    print("YES")
else:
    print("NO")`},
			{Type: "text", Content: `Задание: прочитайте слово и посчитайте в нём гласные латинские буквы a, e, i, o, u (подсказка: цикл по символам и проверка c in "aeiou"; не забудьте про lower()).

К уроку прикреплены задачи «Переворот строки», «Проверка палиндрома» и «Подсчёт гласных» — решите все три и отметьте урок пройденным.`},
		},
	}

	m["py-functions"] = lessonContentSeed{
		Summary:  "Функции def, return, декомпозиция; НОД и проверка простоты за корень.",
		Problems: []string{"НОД двух чисел", "Проверка простого числа"},
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Функция объявляется словом def, результат возвращается через return. Функции позволяют разбить решение на понятные части и переиспользовать код.

Ниже — алгоритм Евклида для наибольшего общего делителя. Он вам ещё много раз пригодится (в стандартной библиотеке есть готовый math.gcd, но олимпиадник обязан понимать, как он работает).`},
			{Type: "code", Language: "python", Caption: "НОД: своя функция и math.gcd. Введите: 12 8", Content: `import math

def gcd(a, b):
    while b != 0:
        a, b = b, a % b
    return a

a, b = map(int, input().split())
print(gcd(a, b))
print(math.gcd(a, b))  # встроенный вариант для сравнения`},
			{Type: "text", Content: `Обратите внимание на строку a, b = b, a % b — Python умеет обменивать значения без временной переменной.

Функции с результатом True/False удобны для проверок. Проверяя число на простоту, достаточно перебирать делители до корня из n: если у n есть делитель больше корня, то парный ему делитель обязательно меньше корня.`},
			{Type: "code", Language: "python", Caption: "Проверка простоты за корень. Введите: 97", Content: `def is_prime(n):
    if n < 2:
        return False
    i = 2
    while i * i <= n:
        if n % i == 0:
            return False
        i += 1
    return True

n = int(input())
print("YES" if is_prime(n) else "NO")`},
			{Type: "text", Content: `Задание: напишите функцию digit_sum(n), возвращающую сумму цифр числа (цифры снимаются через n % 10 и n //= 10).

Затем решите прикреплённые задачи «НОД двух чисел» и «Проверка простого числа» — и отметьте урок пройденным.`},
		},
	}

	m["py-collections"] = lessonContentSeed{
		Summary: "Словари и множества: подсчёт повторов, уникальность, Counter.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Два инструмента, которые выводят решения на новый уровень:

• Словарь dict хранит пары «ключ → значение» и находит значение по ключу мгновенно (за O(1)). Типичное применение — подсчёт: сколько раз встречается каждое число или слово.
• Множество set хранит только уникальные значения, проверка x in s тоже мгновенная. Типичное применение — «сколько различных?» и быстрые проверки на принадлежность.`},
			{Type: "code", Language: "python", Caption: "Подсчёт повторов словарём. Введите: 1 2 2 3 3 3", Content: `nums = list(map(int, input().split()))

count = {}
for x in nums:
    if x in count:
        count[x] += 1
    else:
        count[x] = 1

for key in sorted(count):
    print(key, "встречается", count[key], "раз")`},
			{Type: "text", Content: `Множество создаётся из списка одной командой set(nums) — дубликаты исчезают сами. А для подсчёта повторов в стандартной библиотеке есть готовый Counter — он делает то же, что словарь выше, одной строкой.`},
			{Type: "code", Language: "python", Caption: "set и Counter. Введите: 1 2 2 3 3 3", Content: `from collections import Counter

nums = list(map(int, input().split()))

print("Различных чисел:", len(set(nums)))

c = Counter(nums)
print("Самое частое:", c.most_common(1))`},
			{Type: "text", Content: `Задание: прочитайте строку слов и выведите те слова, которые встречаются ровно один раз (подсказка: Counter + цикл по словам с сохранением порядка).

Отметьте урок пройденным — остался финальный урок курса.`},
		},
	}

	m["py-competitive"] = lessonContentSeed{
		Summary: "Быстрый ввод через sys.stdin, ловушки Python на олимпиадах и checklist.",
		Blocks: []contentBlockSeed{
			{Type: "text", Content: `Python лаконичен, но медленнее C++ в десятки раз. Чтобы он выживал на олимпиадах, нужно знать несколько приёмов.

Первый и главный — быстрый ввод. Обычный input() медленный; при больших объёмах данных (сотни тысяч строк) решение может не уложиться в лимит только из-за чтения. Замена — sys.stdin.`},
			{Type: "code", Language: "python", Caption: "Шаблон с быстрым вводом. Введите: 4, затем 10 20 30 40", Content: `import sys
input = sys.stdin.readline

n = int(input())
nums = list(map(int, input().split()))

out = []
total = 0
for x in nums:
    total += x
    out.append(str(total))

print(" ".join(out))`},
			{Type: "text", Content: `Что здесь важно:

• input = sys.stdin.readline — подменяет медленный input быстрым (осторожно: readline оставляет в конце символ перевода строки, для чисел это не мешает, для строк используйте .strip());
• ответы копятся в списке и выводятся одним print через join — тысячи отдельных print работают заметно медленнее.

Ловушки Python, о которых нужно знать заранее:

1. Глубокая рекурсия падает: лимит по умолчанию около 1000 вызовов. Увеличивайте sys.setrecursionlimit(300000) или переписывайте на цикл.
2. Если алгоритм правильный, но время всё равно не проходит, — сначала ищите лишний цикл у себя, и только потом вините язык.
3. Целые числа не переполняются — об этом в Python можно не думать вовсе.`},
			{Type: "text", Content: `Checklist перед отправкой:

1. Крайние случаи: n = 1, пустые данные, отрицательные числа.
2. Правильные ли // и % (а не /)?
3. Формат вывода точно совпадает с условием?

Курс завершён — поздравляем! Откройте раздел «Обучение» и начинайте «Путь олимпиадника» с Уровня 1. Удачи!`},
		},
	}
}
